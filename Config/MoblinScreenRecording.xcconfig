#include "Base.xcconfig"

PRODUCT_BUNDLE_IDENTIFIER = $(BASE_PRODUCT_BUNDLE_IDENTIFIER).Moblin-Capture
CODE_SIGN_ENTITLEMENTS = Moblin Screen Recording/Moblin Screen Recording.$(CAPABILITIES).entitlements
