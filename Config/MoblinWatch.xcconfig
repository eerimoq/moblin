#include "Base.xcconfig"

PRODUCT_BUNDLE_IDENTIFIER = $(BASE_PRODUCT_BUNDLE_IDENTIFIER).Watch
INFOPLIST_KEY_WKCompanionAppBundleIdentifier = $(BASE_PRODUCT_BUNDLE_IDENTIFIER)
CODE_SIGN_ENTITLEMENTS = Moblin Watch/MoblinWatch.$(CAPABILITIES).entitlements
