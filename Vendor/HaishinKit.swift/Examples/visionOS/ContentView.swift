import HaishinKit
import RTMPHaishinKit
import SRTHaishinKit
import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            PlaybackView()
        }
    }
}

#Preview(windowStyle: .automatic) {
    ContentView()
}
